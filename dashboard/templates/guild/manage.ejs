<meta name="viewport" content="width=device-width, initial-scale=1.0"> <%- include('../blocks/header', {bot, user, path}) %><div class="container">	<div class="row justify-content-md-center">		<h2 style="color: #fff;">Sunucu Yönetimi</h2>	</div>	<div class="row">		<div class="col col-4">			<%- include('../blocks/guild-card', {guild}) %>		</div>		<div class="col">			<%- include('../blocks/guild-nav.ejs', {active: "manage", guild}) %>			<form method="post">				<ul class="list-group list-group-flush">					<li class="list-group-item" style="padding: 0; background-color: #525656;"> 						<div class="input-group mb-2 mr-sm-2 mb-sm-0" style="width: 100%; margin: 0 !important;"> <div class="input-group-addon" style="width: 150px; text-align: center; margin: 5.5px; color: #ffffff; font-size: 15px;">Sunucu Prefixi</div> <% if (bot.ayar.has(`prefix_${guild.id}`) === false) { %>								<input type="text" class="form-control" style="text-align: left; border:2px solid #456879; border-radius:10px;" name="onek" id="onek"									placeholder="Lütfen ayarlamak istediğiniz prefixi yazınız!"> <% } %> 							<% if(bot.ayar.has(`prefix_${guild.id}`) === true) { %>								<input type="text" class="form-control" style="text-align: left; border:2px solid #456879; border-radius:10px;" name="onek" id="onek" value="<%= bot.ayar.fetch(`prefix_${guild.id}`) %>"									placeholder="Lütfen ayarlamak istediğiniz prefixi yazınız!"> <% } %>						</div> <div class="input-group mb-2 mr-sm-2 mb-sm-0" style="width: 100%; margin: 0 !important;"> <div class="input-group-addon" style="width: 150px; text-align: center; margin: 5.5px; color: #ffffff; font-size: 15px;">Giriş Çıkış Kanalı</div> <% if (bot.ayar.has(`gc_${guild.id}`) === false) { %>								<input type="text" class="form-control" style="text-align: left; border:2px solid #456879; border-radius:10px;" name="gc" id="gc"									placeholder="Lütfen ayarlamak istediğiniz kanalı yazınız!"> <% } %> 							<% if(bot.ayar.has(`gc_${guild.id}`) === true) { %>								<input type="text" class="form-control" style="text-align: left; border:2px solid #456879; border-radius:10px;" name="gc" id="gc" value="<%= bot.channels.get(bot.ayar.fetch(`gc_${guild.id}`).replace("<#", "").replace(">", "")).name %>"									placeholder="Lütfen ayarlamak istediğiniz kanalı yazınız!"> <% } %>						</div> <div class="input-group mb-2 mr-sm-2 mb-sm-0" style="width: 100%; margin: 0 !important;"> <div class="input-group-addon" style="width: 150px; text-align: center; margin: 5.5px; color: #ffffff; font-size: 15px;">Moderasyon Kayıtları Kanalı</div> <% if (bot.ayar.has(`mLog_${guild.id}`) === false) { %>								<input type="text" class="form-control" style="text-align: left; border:2px solid #456879; border-radius:10px;" name="modlog" id="modlog"									placeholder="Lütfen ayarlamak istediğiniz kanalı yazınız!"> <% } %> 							<% if(bot.ayar.has(`mLog_${guild.id}`) === true) { %>								<input type="text" class="form-control" style="text-align: left; border:2px solid #456879; border-radius:10px;" name="modlog" id="modlog" value="<%= bot.channels.get(bot.ayar.fetch(`mLog_${guild.id}`).replace("<#", "").replace(">", "")).name %>"									placeholder="Lütfen ayarlamak istediğiniz kanalı yazınız!"> <% } %>						</div> <div class="input-group mb-2 mr-sm-2 mb-sm-0" style="width: 100%; margin: 0 !important;"> <div class="input-group-addon" style="width: 150px; text-align: center; margin: 5.5px; color: #ffffff; font-size: 15px;">Sunucu Kayıtları Kanalı</div> <% if (bot.ayar.has(`log_${guild.id}`) === false) { %>								<input type="text" class="form-control" style="text-align: left; border:2px solid #456879; border-radius:10px;" name="log" id="log"									placeholder="Lütfen ayarlamak istediğiniz kanalı yazınız!"> <% } %> 							<% if(bot.ayar.has(`log_${guild.id}`) === true) { %>								<input type="text" class="form-control" style="text-align: left; border:2px solid #456879; border-radius:10px;" name="log" id="log" value="<%= bot.channels.get(bot.ayar.fetch(`log_${guild.id}`).replace("<#", "").replace(">", "")).name %>"									placeholder="Lütfen ayarlamak istediğiniz kanalı yazınız!"> <% } %>						</div> <div class="input-group mb-2 mr-sm-2 mb-sm-0" style="width: 100%; margin: 0 !important;"> <div class="input-group-addon" style="width: 150px; text-align: center; margin: 5.5px; color: #ffffff; font-size: 15px;">Otomatik Rol</div> <% if (bot.ayar.has(`otoR_${guild.id}`) === false) { %>

